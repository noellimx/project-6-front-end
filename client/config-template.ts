const config = {
  env: "production", // or test
  wsserver: "localhost:8080/ws", // without specify scheme // "______:8080/ws" , "mockserver:8080/ws"
  httpsserver: "fruitt.art:8443"
};

export default config;
